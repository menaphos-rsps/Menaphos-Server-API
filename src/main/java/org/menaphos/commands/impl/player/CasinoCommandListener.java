package org.menaphos.commands.impl.player;

import org.apache.commons.cli.CommandLine;
import org.menaphos.commands.CommandContext;
import org.menaphos.commands.CommandListener;

public class CasinoCommandListener extends CommandListener {



    @Override
    public void execute(CommandLine cmd, CommandContext ctx) {
//        final Player player = ctx.getAuthor();

//        if(player.getClanId() > 0) {
//            player.getLocalCharacter().setCasino(Optional.of(new Casino(player.getPlayerName(),ClanChatHandler.clans[player.getClanId()])));
//            player.getPA().sendMessage("You've created a casino!");
//        } else {
//            player.getPA().sendMessage("You must own a clan to create a casino.");
//        }
    }
}
